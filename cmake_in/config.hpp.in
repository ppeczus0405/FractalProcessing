#include <filesystem>
#include <string>

namespace Config
{
    constexpr char OS_SEPARATOR = static_cast<char>(std::filesystem::path::preferred_separator);
    const std::string SAVE_PATH = string("${CMAKE_CURRENT_SOURCE_DIR}") + OS_SEPARATOR + "generated_images";
}